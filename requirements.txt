# Concrete, pinned environment — reproduces this machine exactly.
# (pyproject.toml declares the abstract dependencies; this file pins them.)
#
# The +cu121 PyTorch wheels are not on plain PyPI, so the PyTorch index is added
# alongside it. Install with:  pip install -r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.5.1+cu121
torchvision==0.20.1+cu121
matplotlib==3.11.1
ipykernel==7.3.0
